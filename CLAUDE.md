# CLAUDE.md

See [AGENTS.md](AGENTS.md) for everything: what this tool does, the one-line installer, verification steps, file locations, and how to develop and release.

Short version: Claude KeepAwake keeps the Mac awake while local Claude sessions run. Install with:

```bash
curl -fsSL https://raw.githubusercontent.com/orbilyte/claude-keepawake/main/install.sh | bash
```
